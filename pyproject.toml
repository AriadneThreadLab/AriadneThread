[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "osm-geoagent"
version = "0.1.0"
description = "Ariadne Thread: knowledge-grounded geographic analysis with AI agents, OpenStreetMap, and explainable workflows."
readme = "README.md"
requires-python = ">=3.10,<3.11"
license = { text = "MIT" }

dependencies = [
    "fastapi>=0.115,<0.116",
    "uvicorn[standard]>=0.30,<0.35",
    "jinja2>=3.1,<4",
    "pydantic>=2.7,<3",
    "pydantic-settings>=2.3,<3",
    "sqlalchemy[asyncio]>=2.0.30,<2.1",
    "asyncpg>=0.29,<0.31",
    "alembic>=1.13,<2",
    "pgvector>=0.3,<0.5",
    "geoalchemy2>=0.15,<0.18",
    "httpx>=0.27,<0.29",
    "openai>=1.40,<2",
    "pyyaml>=6,<7",
]

[project.optional-dependencies]
# Heavy ML stack. Deliberately optional so that unit tests and CI never pull
# torch, and so the embedding model is only installed where it is actually run.
embeddings = [
    "sentence-transformers>=3.0,<4",
]
# Optional energy-network data connector. Not required for OSM analysis.
energy = [
    "simbench>=1.3",
    "pandapower>=2.10",
]
dev = [
    "pytest>=8.2,<9",
    "pytest-asyncio>=0.23,<0.25",
    "ruff>=0.5,<0.13",
    "mypy>=1.10,<2",
]

[project.urls]
Homepage = "https://github.com/AriadneThreadLab/ariadne-thread"
Repository = "https://github.com/AriadneThreadLab/ariadne-thread"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.setuptools.package-data]
"app.web" = ["templates/*.html", "static/css/*", "static/js/*", "static/img/*"]
"app.indicators" = ["definitions/*/*.yaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = ["error::DeprecationWarning:app.*"]
markers = [
    "integration: real model/database checks (enable with RUN_INTEGRATION=1)",
    "avalai: live AvalAI smoke (enable with RUN_AVALAI_SMOKE=1)",
]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["app", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ANN", "RUF"]
ignore = ["ANN401"]

[tool.ruff.lint.per-file-ignores]
# Test doubles and fixtures are readable without full annotations; application
# code stays fully annotated.
"tests/*" = ["ANN001", "ANN002", "ANN003", "ANN201", "ANN202", "ANN204"]
"alembic/*" = ["ANN"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_configs = true
plugins = ["pydantic.mypy"]
exclude = ["^alembic/"]

[[tool.mypy.overrides]]
module = ["sentence_transformers.*", "pgvector.*", "yaml"]
ignore_missing_imports = true

# Application code is strictly typed; test doubles may stay untyped.
[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_calls = false
warn_unused_ignores = false
